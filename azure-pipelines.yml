# Xamarin.iOS
# Build a Xamarin.iOS project.
# Add steps that install certificates, test, sign, and distribute an app, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/xamarin

trigger:
- master

pool:
  vmImage: 'macos-latest'

steps:

# To manually select a Xamarin SDK version on the Microsoft-hosted macOS agent,
# configure this task with the *Mono* version that is associated with the
# Xamarin SDK version that you need, and set the "enabled" property to true.
# See https://go.microsoft.com/fwlink/?linkid=871629
#- script: sudo $AGENT_HOMEDIRECTORY/scripts/select-xamarin-sdk.sh 5_12_0
#  displayName: 'Select the Xamarin SDK version'
#  enabled: false

- task: Bash@3
  inputs:
    script: pwd; ls -lR 
  displayName: Downloading the Wasmer Native Runtimes

- task: Bash@3
  inputs:
    script: make fetch-runtimes
  displayName: Downloading the Wasmer Native Runtimes

- task: Bash@3
  inputs:
    script: msbuild /p:Configuration=Release WasmerSharp/WasmerSharp.csproj
  displayName: Build WasmerSharp

- task: PublishBuildArtifacts@1
  inputs: 
    pathtoPublish: WasmerSharp/bin/Release
    ArtifactName: drop